<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <data noupdate="0">
        <!-- Add you code here -->
        <!-- property.amenities form view -->
        <record id="amenities_view_form" model="ir.ui.view">
            <field name="name">Amenities</field>
            <field name="model">property.amenities</field>
            <field name="arch" type="xml">
                <form string="Amenities">
                    <sheet>
                        <group>
                            <!-- Add your fields here -->
                            <field name="name"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- property.amenities tree view -->
        <record id="amenities_view_tree" model="ir.ui.view">
            <field name="name">Amenities</field>
            <field name="model">property.amenities</field>
            <field name="arch" type="xml">
                <tree>
                    <!-- Add your fields here -->
                    <field name="name"/>
                </tree>
            </field>
        </record>

        <!-- property.amenities action window -->
        <record id="amenities_action" model="ir.actions.act_window">
            <field name="name">Amenities</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">property.amenities</field>
            <field name="view_mode">tree,form</field>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                    <!-- Add Text Here -->
                </p><p>
                    <!-- More details about what a user can do with this object will be OK --> 
                </p>
            </field>
        </record>
    </data>
</odoo>